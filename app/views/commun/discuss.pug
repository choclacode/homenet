extends ../layout.pug

block title
  | Discuss

block content
  section#chatsect
    ul#chatbox
    form#sendmsg
      input(type="text", name="msg", placeholder="Type a message...", autocomplete="off")
      input(type="submit", value="Send")
